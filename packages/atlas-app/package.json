{
  "name": "atlas-app",
  "version": "1.0.0",
  "private": true,
  "description": "Atlas — application de terrain (Android). Enveloppe Capacitor autour du meme code que le widget.",
  "scripts": {
    "vendoriser": "node scripts/vendoriser.mjs",
    "icones": "node scripts/icones.mjs",
    "sync": "npm run vendoriser && npx cap sync android && npm run permissions",
    "apk": "npm run sync && npm run icones && cd android && ./gradlew assembleDebug",
    "test": "node --test scripts/*.test.mjs",
    "permissions": "node scripts/permissions.mjs"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
